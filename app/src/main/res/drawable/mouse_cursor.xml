<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="36dp"
    android:viewportWidth="24"
    android:viewportHeight="36">
    <!-- Stable, classic macOS/Windows style cursor -->
    <!-- Adjusted path so the tip of the pointer is EXACTLY at (0,0) -->
    <!-- This fixes the visual offset where clicks were happening to the left of the tip -->

    <!-- Outer Black Border for contrast -->
    <path
        android:fillColor="#000000"
        android:pathData="M0,0 L16,15 L10,16 L14,24 L11,25 L7,17 L1,22 Z" />

    <!-- Inner White Fill -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M2,3 L12,13 L8,14 L12,22 L10,23 L6,15 L2,19 Z" />
</vector>
